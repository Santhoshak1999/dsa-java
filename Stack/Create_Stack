// "static void main" must be defined in a public class.
public class Main {
    public static void main(String[] args) {
        //A stack is a linear data structure that follows the Last In, First Out (LIFO) principle, where elements are inserted and removed from one end only, known as the top
        //create a interger stack
        Stack<Integer> stack = new Stack();
        stack.push(10);
        stack.push(50);
        stack.push(100);
        //Last In, First Out (LIFO)
        for(int i=0;i<3;i++){
            System.out.println(stack.pop());
        }
        //stack does not have index values in the same way that arrays do. In a stack, elements are accessed and removed according to the Last In, Fisrst Out (LIFO)
    }
}
